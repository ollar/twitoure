import imageResize from 'twitoure/utils/image-resize';
import { module, test } from 'qunit';

module('Unit | Utility | image-resize', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = imageResize();
    assert.ok(result);
  });
});
