<div
    title={{if title title (t 'messages.drop_image_here')}}
    class="image-drop-area"
    ondragenter={{action 'dragEnter'}}
    ondragover={{action 'dragOver'}}
    ondragleave={{action 'dragLeave'}}
    ondrop={{action 'drop'}}
>
    <input
        title={{if title title (t 'messages.drop_image_here')}}
        type="file"
        id="file-input"
        accept="image/*"
        onchange={{action 'inputChange'}} />
    {{yield}}
</div>